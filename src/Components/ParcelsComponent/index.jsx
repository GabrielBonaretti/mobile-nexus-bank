import { View } from "react-native";
import { Background } from "./style";

const ParcelsComponent = ({ item }) => {
  return (
  <Background>
    <View></View>
  </Background>
  );
};

export default ParcelsComponent;
